spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://localhost:3306/synekart
    username: root
    password: Synechron@1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true

server:
  port: 8081